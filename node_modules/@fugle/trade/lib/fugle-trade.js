"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.FugleTrade = void 0;
const utils_1 = require("./utils");
const client_1 = require("./client");
class FugleTrade extends client_1.Client {
    constructor(options = {}) {
        const config = Object.assign(Object.assign({}, (options.configPath ? (0, utils_1.readConfigFile)(options.configPath) : {})), (options.config || {}));
        if (!options.configPath && !options.config)
            throw new TypeError('One of the "configPath" or "config" options must be specified');
        if (!config.apiUrl)
            throw new TypeError('Invalid configuration: "config.apiUrl" is required');
        if (!config.apiKey)
            throw new TypeError('Invalid configuration: "config.apiKey" is required');
        if (!config.apiSecret)
            throw new TypeError('Invalid configuration: "config.apiSecret" is required');
        if (!config.certPath)
            throw new TypeError('Invalid configuration: "config.certPath" is required');
        if (!config.aid)
            throw new TypeError('Invalid configuration: "config.aid" is required');
        super(config);
    }
}
exports.FugleTrade = FugleTrade;
